---
title: "Hands-on Exercise 1"
author: "Eugene Toh"
---

# Getting Started

## Installing and launching R packages

The code chunk below uses `p_load()` of `pacman` to check if `tidyverse` packages are installed in the computer. If they are, they will be loaded by R.

```{r}
pacman::p_load(tidyverse)
```
